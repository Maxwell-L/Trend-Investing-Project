spring:
  application:
    name: Index-Config-Server
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://gitee.com/Maxwell-L/TrendConfig
          search-paths: repo
          skip-ssl-validation: true
          username: Maxwell-L
          password: enjoyMi6n8g


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
